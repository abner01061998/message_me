<div class="ui small menu inverted">
    <div class="ui container">
        <%= link_to  "Chat room", root_path, class: "active item"%>
        <a class="item">
            Messages
        </a>
        <div class="right menu">
            <div class="ui dropdown item">
            
            <%= @current_user ? @current_user.username : "Account" %> <i class="dropdown icon"></i>
            <div class="menu">
                <%= (link_to  "Log out", destroy_session_path, data: { turbo_method: :delete }, class: "item" ) if logged_in? %>
                <%= (link_to  "Log in", new_session_path, class: "item")  if !logged_in? %>
            </div>
            </div>
            <div class="item">
                <% if !logged_in? %>
                    <div class="ui primary button">Sign Up</div>
                <% end %>
            </div>
        </div>
    </div>
  </div>